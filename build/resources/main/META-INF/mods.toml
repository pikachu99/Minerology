modLoader="javafml" #mandatory

loaderVersion="[36,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.
license="MIT"
issueTrackerURL="http://my.issue.tracker/" #optional

[[mods]] #mandatory

modId="minerology" #mandatory

version="${file.jarVersion}" #mandatory

displayName="Minerology" #mandatory

updateJSONURL="http://myurl.me/" #optional

displayURL="http://example.com/" #optional

logoFile="examplemod.png" #optional

credits="Thank you." #optional

authors="TheRedInventor" #optional

description='''
A mod that adds tons of ores, tools, weapons, structures and much more. Many ores and rock are based on Real-Life ones, and others are either based on Youtubers, Games, Friends, etc.
'''

[[dependencies.minerology]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"

[[dependencies.minerology]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.16.5]"
    ordering="NONE"
    side="BOTH"
